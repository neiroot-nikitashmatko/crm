DATABASE_URL=postgres://postgres:postgres@localhost:5432/proclients?sslmode=disable
HTTP_ADDR=:8080
CORS_ORIGINS=http://localhost:5173
JWT_SECRET=local-dev-jwt-secret-change-me-before-production
JWT_TTL_HOURS=24
# BEELINE_WEBHOOK_DEBUG=true
# BEELINE_CREATED_BY_USER_ID=

# Avito Messenger
# AVITO_CLIENT_ID=
# AVITO_CLIENT_SECRET=
# AVITO_USER_ID=
# AVITO_WEBHOOK_SECRET=
# AVITO_CREATED_BY_USER_ID=   # optional, fallback to BEELINE_CREATED_BY_USER_ID
# AVITO_WEBHOOK_DEBUG=false

# MAX messenger (напоминания об оплатах в группу, ежедневно в 10:00 Europe/Moscow)
# MAX_BOT_TOKEN=
# MAX_CHAT_ID=
# MAX_TLS_INSECURE=false  # true только локально на macOS, если TLS к platform-api2.max.ru падает без -k
